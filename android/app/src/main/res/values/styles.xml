<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base de la aplicación. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
      Tema real de la app. DayNight para que la barra de navegación del sistema
      acompañe el modo claro/oscuro que el usuario elija dentro de la app.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
      Pantalla de carga. Los tres atributos `windowSplashScreen*` son los que
      usa Android 12 en adelante; `android:background` cubre las versiones
      anteriores. `postSplashScreenTheme` evita el parpadeo al entrar.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
